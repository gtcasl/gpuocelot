AC_INIT(fm/fm_formats.c)
AC_CONFIG_AUX_DIR(config)
AM_INIT_AUTOMAKE(ffs_release,1.0)
AM_MAINTAINER_MODE
AC_ARG_WITH(deb-build, [  --with-deb-build        Assume necessary files in CERCS packages will be installed],ac_configure_args="$ac_configure_args --with-deb-build")
AC_CONFIG_SUBDIRS(fm cod ffs)
LT_INIT
AC_CONFIG_FILES([Makefile])
AC_OUTPUT
